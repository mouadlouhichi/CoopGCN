# Expected values are design thresholds, not results

`expected_validation_targets.csv` contains pre-specified settings and acceptance
thresholds used to plan missing experiments. It intentionally contains no
projected NDCG, Tail Recall, Coverage, runtime, or attribution outcome.
Inventing such values would not satisfy peer review and must not be presented
as measured evidence.
